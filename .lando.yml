name: simple-voting-system
recipe: drupal11
config:
  webroot: web
  xdebug: true
tooling:
  phpcs:
    service: appserver
    cmd: /app/vendor/bin/phpcs
  phpcbf:
    service: appserver
    cmd: /app/vendor/bin/phpcbf
